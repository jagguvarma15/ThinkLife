# Keycloak Configuration from Docker
# Your Keycloak container is running on: http://localhost:8080

# Step 1: Access Keycloak Admin Console
# URL: http://localhost:8080
# Default credentials: admin / admin (check your docker setup)

# Step 2: Create or select a Realm
# - Go to top-left dropdown, select or create realm (e.g., "thinklife")
NEXT_PUBLIC_KEYCLOAK_REALM="thinklife"

# Step 3: Create a Client
# - Go to: Clients -> Create client
# - Client type: OpenID Connect
# - Client ID: thinklife-frontend
# - Client authentication: OFF (Public client)
# - Valid redirect URIs: http://localhost:3000/*
# - Web origins: http://localhost:3000
NEXT_PUBLIC_KEYCLOAK_CLIENT_ID="thinklife-frontend"

# Step 4: Keycloak URL (from your Docker container)
NEXT_PUBLIC_KEYCLOAK_URL="http://localhost:8080"
